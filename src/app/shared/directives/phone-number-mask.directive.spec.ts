import { PhoneNumberMaskDirective } from './phone-number-mask.directive';

describe('PhoneNumberMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumberMaskDirective();
    expect(directive).toBeTruthy();
  });
});
